{{# with leave ~}}
  {{~# if this.get_leave_type_name ~}}{{~ this.get_leave_type_name }} {{/if~}}
  з {{# with this.get_start_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (до обіду){{/if~}}
    {{~# if this.is_afternoon_leave}} (після обіду){{/if~}}
  {{~/with}} по {{# with this.get_end_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (до обіду){{/if~}}
    {{~# if this.is_afternoon_leave}} (після обіду){{/if~}}
  {{~/with~}}
{{~/with~}}
